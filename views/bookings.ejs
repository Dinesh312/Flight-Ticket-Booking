<%- include('partials/header')  %> 

    </div>

    <div class="table-container container-fluid ">
        <table class="table table-striped table-bordered">
            <caption>List of bookings made</caption>
            <thead class="thead-dark">
                <tr>
                    <th scope="col">TicketID</th>
                    <th scope="col">User Name</th>
                    <th scope="col">Trip Preference</th>
                    <th scope="col">Flying from</th>
                    <th scope="col">Flying to</th>
                    <th scope="col">Departing Date</th>
                    <th scope="col">Return Date</t>
                    <th scope="col">No. of Adults</th>
                    <th scope="col">No. of Children</th>
                    <th scope="col">Class Preference</th>
                </tr>
            </thead>
 
            <tbody>

                <% userTravels.forEach(slot => { %> 
                    <tr>
                        <th scope="row"> <%= slot._id %> </th>
                        <td><%= userName %></td>
                        <td><%= slot.tripPreference %></td>
                        <td><%= slot.from %></td>
                        <td><%= slot.to %></td>
                        <td><%= slot.departingDate %></td>
                        <td><%= slot.returningDate%></td>
                        <td><%= slot.noOfAdults %></td>
                        <td><%= slot.noOfChildren %></td>
                        <td><%= slot.classPreference %></td>
                    </tr>
                <% }); %>
                
            </tbody>    
        </table>

<%- include('partials/footer')  %>  
